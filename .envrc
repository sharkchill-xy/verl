#!/usr/bin/env bash

# 自动激活 uv 虚拟环境
source .venv/bin/activate

# 可选：设置项目相关的环境变量
export PYTHONPATH="${PWD}:${PYTHONPATH}"
export PROJECT_ROOT="${PWD}"

# echo "🚀 已激活 verl 虚拟环境 (Python $(python --version 2>&1 | cut -d' ' -f2))" 
unset PS1